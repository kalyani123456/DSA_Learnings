class Solution {
    public int diagonalSum(int[][] a) {
        int sum = 0;
        for(int i=0,j=a.length-1;i<=a.length-1;i++,j--){
            sum  = sum + a[i][i];
            if(i!=j){
                sum =  sum +a[i][j];
            }
        }
        return sum;
    }
}
